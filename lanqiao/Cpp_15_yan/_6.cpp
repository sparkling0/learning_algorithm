#include <iostream>
#include <algorithm>
#include <vector>
#include <map>
using namespace std;

const int N = 1e5 + 10;
int n, m ,t;
int a[N], f[N];

int main()
{
    cin >> n >> m >> t;
    for(int i = 0; i < n; i ++) cin >> a[i];
    for(int i = 0; i < n; i ++) cin >> a[i];
    return 0;
}